apiVersion: v1
kind: ServiceAccount
metadata:
  name: chatbot-sa
  namespace: chatbot
  annotations:
    # For AWS EKS with IRSA (IAM Roles for Service Accounts)
    # Replace with your actual IAM role ARN
    eks.amazonaws.com/role-arn: arn:aws:iam::YOUR_ACCOUNT_ID:role/chatbot-eks-role

